import React from 'react';
import { Header } from './type';

export const AppHeader = ({ title }: Header) => {
    return (
        <div>
            <h1>Header</h1>
        </div>
    );
};
